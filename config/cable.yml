development:
  adapter: redis #async
  url: redis://localhost:3000

test:
  adapter: redis
  url: redis://localhost:3000

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: AutoSchool_production
